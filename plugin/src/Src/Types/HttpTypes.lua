export type ConnectionResult = {
    id: string,
    sessionInfo: SessionInfo,
}

export type SessionInfo = {
	licenseType: string,
	sessionType: string,
	connectionType: string,
	pipePath: string?,
}

return nil
